import { createMuiTheme } from "@material-ui/core/styles";

const theme = createMuiTheme({
  palette: {
    primary: {
      main: `#ff9800`,
      second: `#bdccdb`,
    },
    secondary: {
      main: `#115293`,
    },
  },
});

export default theme;
